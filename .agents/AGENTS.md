# inkCRM Agent Rules

- **Do NOT push changes to Git automatically**: Do not run `git push` unless the user explicitly requests or authorizes it. Staging (`git add`) and committing (`git commit`) locally is fine for tracking progress, but remote pushing must be done only upon explicit instructions.

